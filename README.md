Leansw GOCD compose repo
=============

## Requirements

* docker
* docker-compose

## Usage

you should change to local go user and add docker permission to go

```
sudo usermod -aG docker go

su go

docker-compose up -d

```
